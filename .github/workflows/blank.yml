name: Build and Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@latest

    - name: Setup Node.js
      uses: actions/setup-node@latest
      with:
        node-version: '20'

    - name: Install dependencies
      run: npm ci

    - name: Build website
      run: |
        npm run build 2>&1 | tee build.log
        EXIT_CODE=$?
        echo "Exit code: $EXIT_CODE"
        cat build.log
        if [ $EXIT_CODE -ne 0 ]; then
          echo "Compile failed with exit code $EXIT_CODE"
          exit $EXIT_CODE
        fi

    - name: Check Uncommitted Changes
      run: |
        if [ -n "$(git status --porcelain)" ]; then
          echo "Error: Uncommitted changes exist. Commit and push your changes before deploying."
          exit 1
        fi

    - name: Archive artifacts
      uses: actions/upload-artifact@latest
      with:
        name: build-artifacts
        path: home/redtouch/RedTouchGTCWebsite/index.html

    - name: Deploy to Hosting Service
      run: |
        # Add deployment commands specific to cPanel or your hosting service here
